<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('homepage')}}">mmmhhh les voitures</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a {% if app.request.attributes.get("_route") =='homepage' %}class="nav-item nav-link active" {% endif %} class="nav-item nav-link" href="{{ path('homepage')}}">Accueil</a>
      <a {% if app.request.attributes.get("_route") =='homepage' %}class="nav-item nav-link active" {% endif %} class="nav-item nav-link" href="{{ path('advert_index')}}">Liste des annonces</a>
      <a {% if app.request.attributes.get("_route") =='homepage' %}class="nav-item nav-link active" {% endif %} class="nav-item nav-link" href="{{ path('advert_new')}}">Nouvelle annonce</a>
                {% if (app.user) %}
                <li> <span class="uk-badge">{{ app.user.username }} </span></li>
                <li><a href="{{ path('app_logout')}}"><span class="uk-badge">Déconnexion</span></a></li>
                {% endif %}
    </div>
  </div>
</nav>